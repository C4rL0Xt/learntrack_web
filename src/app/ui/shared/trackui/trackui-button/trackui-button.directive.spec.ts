import { TrackuiButtonDirective } from './trackui-button.directive';

describe('TrackuiButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackuiButtonDirective();
    expect(directive).toBeTruthy();
  });
});
